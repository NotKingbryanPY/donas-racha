# Donas Control Piloto — APK de prueba

- Archivo actualizado: `Donas-Control-Piloto-1.2.0-pedidos-actualizados.apk`.
- Paquete Android: `com.bryan.donas.pilot` (se instala junto a la app anterior y guarda sus propios datos).
- Versión: `1.2.0-piloto`, Android 6 o posterior.
- Incluye la corrección del teclado de «RESETEAR» y del cierre al abrir «Pedidos».
- «Actualizar pedidos» consulta directamente los pedidos del servidor, aunque la sincronización de ventas locales esté pendiente, e informa los errores de acceso o conexión.
- El APK actualizado usa la misma firma y el mismo paquete que el piloto anterior: puede instalarse encima sin borrar sus datos locales.
- SHA-256 del APK actualizado: `F322F6A0E272496737216D650E81D8CBB597D6728C72E7E273B518E5E7512488`.

La variante piloto es para pruebas. Sus ventas y stock locales son independientes de la aplicación principal; no registres ventas reales en ambas a la vez.
